#include <bits/stdc++.h>  
#define ll long long int   
using namespace std;      

const ll MOD = 998244353;
#define nl "\n"  
#define yes cout << "YES" nl             
#define no cout << "NO" nl                       
#define vap(i, n) for (ll i = 0; i < n; i++)   
#define vapi(i, n) for (ll i = 1; i <= n; i++) 
#define vapii(i, n) for (ll i = 1; i < n; i++) 
#define gcd gcd                        
#define dee(x) cout << #x << " = " << x nl

ll p_mod(ll num, ll idx, ll mod) {             
    
    num = num % mod;
    ll answer = 1;  
    while (idx > 0) { 
        if (idx & 1) 
            answer = (answer * num) % mod; 
        idx = idx >> 1; 
        num= (num * num) % mod; 
    } 
    return answer; 
}

int main()
{
    ios::sync_with_stdio(false);  
    cin.tie(NULL);                

    int kr;       
    cin >> kr;     

    while (kr--)  
    {
          
    }

    return 0;  
}

